interface:
  display_name: "Extract and Export Notes"
  short_description: "Extract cross-domain note knowledge and publish web output"
  default_prompt: "Use $extract-and-export-notes to extract candidate knowledge from my changed notes, delegate query and ingestion, and publish the validated result."
