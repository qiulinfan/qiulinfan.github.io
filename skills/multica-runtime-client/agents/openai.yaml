interface:
  display_name: "Multica Runtime Client"
  short_description: "Join Multica over Tailscale with safe VPN split routing"
  default_prompt: "Use $multica-runtime-client to verify Tailscale and VPN coexistence before opening Multica, then join every detected local runtime to the trusted workspace."
