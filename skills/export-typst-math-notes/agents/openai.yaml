interface:
  display_name: "Export Typst Math Notes"
  short_description: "Publish Typst math notes and sync the knowledge graph"
  default_prompt: "Use $export-typst-math-notes to inspect and migrate or export this math note, synchronize the repository knowledge graph, run the fast graph and HTML checks, and publish its web output when requested."
