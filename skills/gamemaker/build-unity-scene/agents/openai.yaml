interface:
  display_name: "Build Unity Scene"
  short_description: "Build and validate Unity scenes from project constraints"
  default_prompt: "Use $build-unity-scene to build and validate a Unity scene from the project's architecture and level requirements."
