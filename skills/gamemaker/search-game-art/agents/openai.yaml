interface:
  display_name: "Search Game Art"
  short_description: "Match design, animation, license, and Unity needs"
  default_prompt: "Use $search-game-art to turn the current game design into asset requirements, find license-verified candidates, and audit or import only when explicitly requested."
