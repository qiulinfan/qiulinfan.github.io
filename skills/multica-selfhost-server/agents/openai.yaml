interface:
  display_name: "Multica Self-host Server"
  short_description: "Deploy a private Multica control plane with fixed-code login"
  default_prompt: "Use $multica-selfhost-server to deploy the private control plane with fixed code 114514, produce credential-free client handoffs, and finalize all Multica-detected host runtimes."
