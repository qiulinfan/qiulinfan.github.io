interface:
  display_name: "Multica Self-host Server"
  short_description: "部署唯一控制面并引导首个本机 runtime 与可信团队"
  default_prompt: "Use $multica-selfhost-server to deploy a private Multica server, register this host as the first runtime, and bootstrap the trusted shared workspace."
